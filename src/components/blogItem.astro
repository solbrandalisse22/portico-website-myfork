---
const { link, title, image } = Astro.props
---

<article class="flex flex-col justify-center items-center">
  <a
    class="relative group flex flex-col gap-y-4 text-center justify-center items-center mt-2 uppercase m-auto transition-all text__glowing"
    href={link}
  >
    <img
      src={image}
      class="aspect-video rounded-lg w-full h-auto transition group-hover:scale-105 object-cover"
      loading="lazy"
    />
    <img
      src={image}
      class="absolute inset-0 opacity-0 brightness-200 blur-xl group-hover:opacity-40 scale-105 aspect-video rounded w-full h-auto -z-10 transition object-cover"
      loading="lazy"
    />
    <h3 class="text-xl my-2 line-clamp-3">{title}</h3>
    <span
      class="border-white inline-flex w-fit border-2 border-solid rounded-full py-2 px-12"
      >Read more</span
    >
  </a>
</article>
