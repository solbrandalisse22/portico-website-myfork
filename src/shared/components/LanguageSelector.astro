---
import { getI18N } from "@/shared/i18n";
import SpainFlag from "@/shared/components/flags/Spain.astro"
import FrenchFlag from "@/shared/components/flags/French.astro"
import UkFlag from "@/shared/components/flags/UkFlag.astro"
import ItalianFlag from "@/shared/components/flags/Italy.astro"
import GermanyFlag from "@/shared/components/flags/Germany.astro"
import ChevronIcon from "@/shared/components/icons/Chevron.astro"

const { currentLocale = "en", url, props } = Astro
const { language } = props

const i18n = getI18N({ language });
const pathname = url.pathname

const restUrl = ''

const LOCALES: Record<
  string,
  { code: string; name: string; href: string; flag: typeof SpainFlag }
> = {
  en: {
    code: "en",
    href: `porticosport.com${restUrl}`,
    name: "English",
    flag: UkFlag,
  },
  es: {
    code: "es",
    href: `https://porticosport.es${restUrl}`,
    name: "Español",
    flag: SpainFlag,
  },
  fr: {
    code: "fr",
    name: "French",
    href: `https://porticosport.fr${restUrl}`,
    flag: FrenchFlag,
  },
  it: {
    code: "it",
    name: "Italian",
    href: `https://porticosport.it${restUrl}`,
    flag: ItalianFlag,
  },
  ge: {
    code: "ge",
    name: "Germany",
    href: `https://porticosport.de${restUrl}`,
    flag: GermanyFlag,
  },
}

const currentLocaleData = LOCALES[currentLocale]
const otherLocales = Object.values(LOCALES).filter(
  (locale) => locale.code !== currentLocale
)
const currentPath = url.pathname.replace(currentLocaleData.href, "")
---

<div class="relative inline-block text-left">
  <div
    class="group text-white rounded-md text-xs font-semibold bg-black/30 hover:bg-black/70 transition-all"
  >
    <button
      type="button"
      class="inline-flex justify-start items-center w-full gap-x-2 px-3 py-2"
      aria-expanded="true"
      aria-haspopup="true"
    >
      <currentLocaleData.flag />
      {currentLocaleData.name}
      <ChevronIcon />
    </button>
    <ul
      class="group-hover:block group-hover:animate-fade-down group-hover:animate-duration-200 hidden pt-0.5 absolute w-full"
    >
      {
        otherLocales.map((locale) => (
          <li class="">
            <a
              class="rounded-md bg-black/30 hover:bg-black/70 whitespace-no-wrap inline-flex justify-start items-center w-full gap-x-2 px-3 py-2"
              href={`${locale.href}${currentPath}`}
            >
              <locale.flag />
              {locale.name}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</div>
