---
import Layout from "@/shared/layouts/Layout.astro";
import HeroContainer from "@/shared/components/HeroContainer.astro";
import Breadcrumb from "@/shared/components/Breadcrumb.astro";
import { getI18N, getCurrentLocale } from "@/shared/i18n"

interface Props {
  language: string
}

const { language } = Astro.props
const i18n = getI18N({ language })
const currentLocale = getCurrentLocale({ language })
---

<Layout title={i18n.PAGES.NO_FOUND.SEO_TITLE} description={i18n.PAGES.NO_FOUND.SEO_DESCRIPTION} currentLocale={currentLocale} language={language}>
  <HeroContainer img="/factory-portico.webp" imgAlt={i18n.PAGES.NO_FOUND.TITLE_ALT} heading={i18n.PAGES.NO_FOUND.DESCRIPTION} />
</Layout>
